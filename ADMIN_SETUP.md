# 🔧 إعداد سكريبت إنشاء Super Admin

## الطريقة 1: استخدام السكريبت (مستحسن)

### الخطوات:

#### 1. تثبيت Firebase Admin SDK

```bash
npm install firebase-admin --save-dev
```

#### 2. تحميل Service Account Key

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك: `center-of-the-centers`
3. اضغط على أيقونة الترس ⚙️ → **Project settings**
4. اذهب إلى تبويب **Service accounts**
5. اضغط **Generate new private key**
6. احفظ الملف باسم `serviceAccountKey.json` في المجلد الرئيسي للمشروع

**⚠️ مهم جداً:**
- لا تشارك هذا الملف مع أحد
- لا ترفعه إلى Git
- أضفه إلى `.gitignore`

#### 3. إضافة الملف إلى .gitignore

```bash
echo "serviceAccountKey.json" >> .gitignore
```

#### 4. تشغيل السكريبت

```bash
node scripts/create-super-admin.js
```

#### 5. أدخل المعلومات المطلوبة

```
📧 البريد الإلكتروني للمسؤول: admin@example.com
🔑 كلمة المرور (6 أحرف على الأقل): YourStrongPass123
👤 الاسم (اختياري): محمد أحمد
```

#### 6. تم! 🎉

سيظهر لك:
```
✅ تم إنشاء حساب المصادقة
✅ تم إنشاء ملف المستخدم في Firestore

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 تم إنشاء Super Admin بنجاح!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 معلومات الحساب:
   البريد: admin@example.com
   UID: xyz123...
   الاسم: محمد أحمد
   الدور: Super Admin

🔗 يمكنك الآن تسجيل الدخول من:
   http://localhost:5173/admin/login
```

---

## الطريقة 2: يدوياً من Firebase Console

### إذا لم تستطع استخدام السكريبت:

#### 1. إنشاء المستخدم في Authentication

1. Firebase Console → **Authentication**
2. **Add user**
3. أدخل:
   - Email: `admin@example.com`
   - Password: `YourStrongPass123`
4. **Add user**
5. **انسخ الـ UID**

#### 2. إنشاء Document في Firestore

1. Firebase Console → **Firestore Database**
2. **Start collection** (إذا كانت فارغة) أو اختر `users`
3. **Add document**
4. Document ID: **الصق الـ UID المنسوخ**
5. أضف الحقول:

| Field | Type | Value |
|-------|------|-------|
| uid | string | `<الصق الـ UID>` |
| email | string | `admin@example.com` |
| role | string | `super_admin` |
| status | string | `active` |
| displayName | string | `مسؤول النظام` |
| createdAt | timestamp | `<اضغط على أيقونة الساعة>` |
| updatedAt | timestamp | `<اضغط على أيقونة الساعة>` |

6. **Save**

#### 3. تم! 🎉

يمكنك الآن تسجيل الدخول من:
```
http://localhost:5173/admin/login
```

---

## استكشاف الأخطاء

### خطأ: "Cannot find module '../serviceAccountKey.json'"

**الحل:**
- تأكد من وجود ملف `serviceAccountKey.json` في المجلد الرئيسي
- تأكد من اسم الملف صحيح (حساس لحالة الأحرف)

### خطأ: "auth/email-already-exists"

**الحل:**
- البريد الإلكتروني مستخدم بالفعل
- جرب بريد إلكتروني آخر أو احذف المستخدم القديم

### خطأ: "auth/weak-password"

**الحل:**
- استخدم كلمة مرور أقوى (6 أحرف على الأقل)

### خطأ: "Permission denied"

**الحل:**
- تأكد من أن Service Account له صلاحيات كافية
- تأكد من أنك استخدمت الـ Service Account الصحيح للمشروع

---

## الأمان 🔒

**⚠️ تحذيرات مهمة:**

1. **لا تشارك `serviceAccountKey.json` أبداً**
   - هذا الملف يعطي صلاحيات كاملة على مشروع Firebase
   - لا ترفعه إلى GitHub أو أي نظام تحكم بالإصدارات

2. **أضف الملف إلى .gitignore**
   ```bash
   echo "serviceAccountKey.json" >> .gitignore
   ```

3. **استخدم كلمات مرور قوية**
   - أحرف كبيرة وصغيرة
   - أرقام ورموز
   - 12 حرف على الأقل للحسابات المهمة

4. **احذف السكريبت بعد الاستخدام (اختياري)**
   - بعد إنشاء Super Admin، يمكنك حذف السكريبت
   - أو احتفظ به لإنشاء مسؤولين إضافيين لاحقاً

---

## الخطوات التالية ➡️

1. ✅ أنشئ Super Admin
2. ✅ سجل دخول من `/admin/login`
3. ✅ ابدأ في إدارة المراكز
4. ✅ أرسل دعوات لمسؤولي المراكز

---

**للمزيد من المعلومات:** اقرأ `RBAC_SETUP.md` و `QUICK_START.md`
